diff --git a//root/workspace/Feishu-Nanobot/nanobot/agent/loop.py b//root/workspace/Feishu-Nanobot/nanobot/agent/loop.py
--- a//root/workspace/Feishu-Nanobot/nanobot/agent/loop.py
+++ b//root/workspace/Feishu-Nanobot/nanobot/agent/loop.py
@@ -764,3 +764,3 @@
         # publish card
-        self._publish_card(msg)
+        await self._publish_card(msg)
 
@@ -827,4 +827,45 @@
 
-    def _publish_card(self, msg: InboundMessage):
-        return 
+    async def _publish_card(self, msg: InboundMessage) -> None:
+        """Publish a Feishu interactive card for the inbound message.
+
+        There is no dedicated "card message" event type on the bus today. We
+        keep using ``OutboundMessage`` and attach a Feishu-specific hint in
+        ``metadata["feishu"]`` so the Feishu channel can force
+        ``msg_type="interactive"`` instead of relying on markdown auto-detect.
+        """
+        if msg.channel != "feishu":
+            return
+
+        content = (msg.content or "").strip()
+        if not content:
+            return
+
+        feishu_meta = dict((msg.metadata or {}).get("feishu") or {})
+        card = feishu_meta.get("card")
+        if not isinstance(card, dict):
+            card = {
+                "config": {"wide_screen_mode": True},
+                "elements": [
+                    {
+                        "tag": "markdown",
+                        "content": f"**[{msg.sender_id}]**\n\n{content}",
+                    }
+                ],
+            }
+
+        outbound_meta = dict(msg.metadata or {})
+        outbound_meta["feishu"] = {
+            **feishu_meta,
+            "msg_type": "interactive",
+            "card": card,
+        }
+
+        await self.bus.publish_outbound(
+            OutboundMessage(
+                channel=msg.channel,
+                chat_id=msg.chat_id,
+                content=content,
+                metadata=outbound_meta,
+            )
+        )
 
diff --git a//root/workspace/Feishu-Nanobot/nanobot/channels/feishu.py b//root/workspace/Feishu-Nanobot/nanobot/channels/feishu.py
--- a//root/workspace/Feishu-Nanobot/nanobot/channels/feishu.py
+++ b//root/workspace/Feishu-Nanobot/nanobot/channels/feishu.py
@@ -1475,2 +1475,32 @@
 
+            feishu_meta = msg.metadata.get("feishu", {}) if isinstance(msg.metadata, dict) else {}
+            explicit_msg_type = feishu_meta.get("msg_type") if isinstance(feishu_meta, dict) else None
+
+            if explicit_msg_type == "interactive":
+                explicit_card = feishu_meta.get("card")
+                if isinstance(explicit_card, str):
+                    await loop.run_in_executor(
+                        None, _do_send, "interactive", explicit_card
+                    )
+                    return
+                if isinstance(explicit_card, dict):
+                    await loop.run_in_executor(
+                        None,
+                        _do_send,
+                        "interactive",
+                        json.dumps(explicit_card, ensure_ascii=False),
+                    )
+                    return
+                if msg.content and msg.content.strip():
+                    elements = self._build_card_elements(msg.content)
+                    for chunk in self._split_elements_by_table_limit(elements):
+                        card = {"config": {"wide_screen_mode": True}, "elements": chunk}
+                        await loop.run_in_executor(
+                            None,
+                            _do_send,
+                            "interactive",
+                            json.dumps(card, ensure_ascii=False),
+                        )
+                    return
+
             if msg.content and msg.content.strip():
